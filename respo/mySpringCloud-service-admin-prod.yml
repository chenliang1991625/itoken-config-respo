spring:
  application:
    name: [project]-service-admin
  datasource:
    druid:
      url: jdbc:mysql://192.168.75.128:3306/[project]-service-admin?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: [dbuser]
      password: [dbpassword]
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8900